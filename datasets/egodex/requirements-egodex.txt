# Dependencies for the egodex facade + runnable demo:
#   egodex.py + egodex_demo.py and their local modules
#   (clip_features.py, egodex_lerobot.py, pose_features.py, skeleton_features.py).
#
#   pip install -r requirements-egodex.txt
#
# NOTE: the HDF5 -> LeRobot convert step (egodex_lerobot.py) needs a Daft build with the
# new Hdf5File API (nightly / >= next release) and the `lerobot` package for the write.
# If you already have a LeRobot dataset, skip convert and you can drop the `lerobot` dep.
daft>=0.7.16           # Hdf5File API: nightly / >= next release
torch
transformers>=5.0
sentencepiece          # SigLIP's text tokenizer
pillow                 # frames -> PIL images for the processor + skeleton overlay
numpy
av                     # PyAV: lerobot.read decodes video frames by timestamp
lerobot                # LeRobotDataset writer (convert step only)
gradio